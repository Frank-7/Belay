# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.env
.belay-prototype/
.belay-purchase-simulator/

# ---- Tooling caches ----
.pytest_cache/
.ruff_cache/
.mypy_cache/

# ---- OS noise ----
.DS_Store
Thumbs.db

# ---- Generated by `make all` ----
# The trace viewer is a build artifact; `make viewer` recreates it in seconds.
viewer/trace.html

# Scratch run directories, if you pass keep=True to run_trial.
/tmp-runs/
*.db
*.db-wal
*.db-shm
journal.jsonl
trace.jsonl
outcome.*.json
perms.json

# ---- Deliberately NOT ignored ----
# results/*.json is committed on purpose. FINDINGS.md quotes those numbers,
# and a reader should be able to check the prose against the data without a
# three-minute rerun. `make clean` preserves them; don't add them here.
